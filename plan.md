# AI 模拟面试原型开发计划（两周版）

> 2026-09-08 题库专项更新：用户已通过 Grill Me 确认[用户题库完善计划](docs/question-bank-plan.md)。题库选题、独立题库管理、导入确认、来源与练习去重以该专项计划为准；以下保留原始 MVP 计划。

> 2026-09-08 实时面试专项更新：用户已确认电脑端 AI 面试官采用多服务商语音方案。OpenAI 与配置了业务空间 WebRTC 地址的千问采用原生语音到语音；DeepSeek、豆包方舟以及未配置千问 Realtime URL 的连接采用自动级联语音。首选 Chrome/Edge，原生模式使用语义 VAD 和 WebRTC，级联模式在短暂停顿后自动提交转写，两者均无需手动确认。页面底部同步显示辅助字幕，断线失败时降级为文字输入。API Key 仅驻留服务器内存，原始音频不落盘。下文中与本更新冲突的旧版“转写确认后提交”设计均由本更新取代。

## 0. 计划结论

本项目在两周内由一人从零开发一个可本地运行、可完整演示的 AI 语音模拟面试原型。

原型的目标不是建设生产级招聘平台，而是验证以下核心假设：

> 系统根据候选人的简历和目标岗位 JD 生成面试计划，通过有依据的动态追问和证据化复盘，能够帮助学生在同能力、不同题目的后测中改善回答质量。

首个正式评测岗位为 **AI 应用开发实习生**。系统允许输入任意岗位 JD，但除首个正式岗位外，统一标记为“实验模式，题目与评分未经完整校准”。

本计划优先保证一条可运行的纵向链路，不承诺生产级稳定性、多人并发、完整岗位覆盖或真实招聘结果预测。

---

## 1. 用户与使用场景

### 1.1 目标用户

- 正在寻找实习的在校学生，重点面向大三、大四和研究生。
- 至少有一段课程项目、个人项目、比赛项目或研究经历。
- 已获得或正在准备一份具体岗位 JD。
- 希望练习技术面和项目深挖，而不是只获取通用题库。

### 1.2 正式岗位

首版正式评测岗位：**AI 应用开发实习生**。

基础能力范围包括：

- Python 与基础软件工程能力；
- 大模型 API 使用与异常处理；
- Prompt 设计和结构化输出；
- RAG 基础流程、向量检索与召回；
- Agent 或工具调用的基本设计；
- 后端接口、数据存储与服务部署基础；
- 对本人项目的目标、架构、贡献、难点和结果进行解释的能力。

正式岗位的基础能力模型只用于兜底。每场面试仍以用户提供的 JD 和简历为主要依据。

### 1.3 任意 JD 模式

用户可以提交其他岗位的 JD，系统仍可完成解析、计划生成、面试和报告，但必须满足以下限制：

- 面试准备页显示“实验模式”；
- 报告注明评分标准未经完整人工校准；
- 未经外部资料验证的题目不计入正式技术正确性得分；
- 不将实验评分与正式岗位结果横向比较。

---

## 2. 两周验证目标

### 2.1 两周内需要证明的事情

1. 用户能从简历和 JD 输入开始，完整完成一场语音模拟面试。
2. 系统能将 JD 归并为有限的高优先级主题，而不是随机出题。
3. 系统能根据回答触发澄清、证据追问、深挖、提示或切换主题。
4. 系统能在结束后输出有回答证据和依据等级的复盘报告。
5. 系统能用同能力、不同题目的前后测初步观察回答质量变化。
6. 网络或第三方能力失败时，演示流程存在可用的降级路径。

### 2.2 两周内不能声称的事情

- 不能声称已经科学证明产品能提高真实面试通过率。
- 不能声称支持的所有岗位都具有可信、统一的评分。
- 不能依据摄像头画面评价眼神、情绪、性格或诚信。
- 不能用四名组员的数据建立“同岗位人群基准”。
- 不能把模型评分包装成真实招聘录用结论。
- 不能声称系统已经达到生产级安全性和稳定性。

### 2.3 验证分层

#### 两周内部验收

- 四名组员每人完成 2～3 场完整测试；
- 用于发现流程、语音、追问、评分和恢复问题；
- 结论只允许表述为“原型链路可用”。

#### 后续外部效果验证

- 招募至少 8～10 名不参与开发的目标学生；
- 使用同能力、不同题目的平行前测和后测；
- 隐藏前测、后测身份后进行评分；
- 至少人工复核部分答案；
- 记录真实面试结果，但只作为 2～6 周后的观察指标。

方向性成功门槛：

- 至少 8 人完成完整实验；
- 至少 60% 的完成人在后测中提升；
- 平均提升至少达到五级量表的 0.5 级；
- 至少 5 人认为报告指出了此前未意识到的问题。

---

## 3. MVP 范围

### 3.1 必须实现

- 上传简历文件，首版支持 PDF、DOCX 和 TXT；
- 保留粘贴简历文本作为解析失败时的兜底方式；
- 粘贴岗位 JD，或选择正式岗位的内置基准 JD；
- 解析并展示候选人画像和岗位画像；
- 允许用户确认、编辑关键解析结果；
- 生成并持久化本场 `InterviewPlan`；
- 面试前展示主题、预计时长、隐私提示和设备检测；
- 浏览器本地摄像头预览；
- 麦克风输入和 OpenAI Realtime 语音到语音对话；
- 语义 VAD 自动判定说话结束，支持用户插话并中断面试官；
- 页面底部实时展示候选人与 AI 面试官的辅助字幕；
- 静态 AI 面试官头像、自然人声和专业、亲和、压力三种风格；
- 基于结构化回答评价的动态追问；
- 主动结束、超时确认和正常结束；
- 带证据、置信度和依据等级的面试报告；
- 降级为文字输入、文字播报或预生成问题；
- 使用匿名会话 ID 保存本地记录；
- 删除单场面试记录。

### 3.2 明确不实现

- 账号、注册、登录和权限体系；
- 云端正式部署和多人并发；
- 扫描版 PDF 的 OCR、图片简历识别和复杂版式还原；
- DOC、PPT、网页简历及压缩包等非首版格式；
- 多人语音房间、电话线路接入和通话录音；
- 实时数字人、口型同步或虚拟人视频；
- 摄像头录像、表情、眼神、姿态或情绪分析；
- 原始音频长期存储；
- 自动抓取招聘网站或运行时全网搜索；
- 完整 RAG、知识图谱或大规模题库；
- 同岗位人群排名和百分位；
- 跨场次能力趋势与职业预测；
- 付费、会员和企业招聘端；
- 微服务、消息队列和复杂分布式架构。

---

## 4. 完整用户流程

```text
进入本地 Web 应用
  ↓
阅读隐私提示并创建匿名会话
  ↓
上传 PDF、DOCX、TXT 简历，或直接粘贴文本
  ↓
系统检查文件类型、大小并提取文本
  ↓
提取失败时提示用户改为粘贴文本
  ↓
选择内置正式岗位，或粘贴任意岗位 JD
  ↓
系统解析 CandidateProfile 和 JobProfile
  ↓
用户确认、修正解析结果
  ↓
系统生成 4～6 个高优先级面试主题和预计时长
  ↓
展示正式/实验模式、主题、时间及规则
  ↓
检查摄像头、麦克风、Realtime 连接和音频播放
  ↓
开始面试：AI 自然语音提问 ↔ 用户直接口述并可随时插话
  ↓
底部同步辅助字幕；后台记录回答 → 选择追问、提示或切题
  ↓
计划主题全部覆盖，或达到预计时间
  ↓
需要时询问用户是否延长，硬上限 60 分钟
  ↓
允许用户反问一次
  ↓
生成复盘报告
  ↓
可选：完成同能力、不同题目的后测
```

用户可随时提前结束。提前结束时仍生成报告，但必须标记为“不完整”，且未覆盖主题显示为“未考察”。

---

## 5. 面试计划与结束条件

### 5.1 “问全”的定义

“问全”不表示穷举岗位的全部知识，而表示：

> `InterviewPlan` 中所有高优先级主题均完成至少一次有效验证。

JD 中语义重复或高度相关的要求必须先归并。默认生成 4～6 个高优先级主题，每个主题包含：

- 主题名称；
- 来源于 JD、简历或岗位基线的证据；
- 本场验证目标；
- 重要权重；
- 初始难度；
- 主问题或候选问题；
- 最多 2 次追问；
- 完成判定条件；
- 预计时间。

### 5.2 时间规则

- 系统在开始前根据主题数量估算时长，通常为 25～40 分钟；
- 用户确认预计时长后开始；
- 到达预计时间但仍有未覆盖主题时，询问是否延长；
- 用户拒绝延长则结束，并标记未考察主题；
- 无论用户是否延长，单场硬上限为 60 分钟；
- 不设置固定问题总数，但每个主题最多追问 2 次；
- 连续追问价值不足、回答重复或模型异常时必须切换主题。

### 5.3 动态动作

系统每轮先生成结构化 `AnswerEvaluation`，再由规则选择动作：

- `clarify`：回答含糊、偏题或存在转写疑点；
- `request_evidence`：观点存在但缺乏项目例子或数据；
- `probe_depth`：结论基本正确，但缺少原理、边界或实现细节；
- `challenge`：提出反例、异常场景或约束变化；
- `give_hint`：核心结论错误或用户卡住；
- `switch_topic`：主题已完成、追问达到上限或继续追问价值不足；
- `end_interview`：全部主题完成、用户结束或达到时间上限。

LLM 负责语义理解、问题措辞和候选评价；确定性规则负责追问上限、覆盖控制、时间控制和合法状态迁移。

---

## 6. 题目、依据与评分

### 6.1 题目来源分级

#### A 级：已验证

- 考察点和评分要点有官方技术文档、权威教材或人工审核支持；
- 可以参与技术正确性评分。

#### B 级：面经启发

- 公开面经只用于识别近期常见考察主题；
- 模型根据用户项目和 JD 重新生成问题；
- 评分点必须另行用可靠资料验证；
- 验证完成后可以参与正式评分。

#### C 级：模型生成、未经验证

- 只用于实验性练习；
- 必须在报告中标记“未核验”；
- 不进入正式技术正确性得分；
- 只能提供表达、完整性、证据性等训练建议。

不得整段复制面经内容。每道题至少保存：

- `source_level`；
- 来源链接或来源说明；
- 查询或审核日期；
- 考察主题；
- 参考评分点；
- 适用岗位；
- 题目、Prompt 和 Rubric 版本。

### 6.2 回答评价维度

- `correctness`：技术或业务结论是否正确；
- `completeness`：是否覆盖问题的关键部分；
- `depth`：是否说明原理、取舍、边界和异常；
- `relevance`：是否回答当前问题；
- `clarity`：表达是否清晰、结构化；
- `evidence`：是否提供真实项目细节、数据或结果；
- `confidence`：系统对本次评价的把握程度。

只有 A、B 级题目可以正式计算 `correctness`。低置信度评价必须降权或标记为需要复核。

### 6.3 分数解释

单轮和主题使用五级量表：

1. 明显不足；
2. 部分体现；
3. 基本达到；
4. 表现良好；
5. 表现突出。

报告不得只显示孤立总分。每个主题必须同时显示：

- 得分和置信度；
- 已考察、未回答或未考察状态；
- 支撑结论的回答片段；
- 评分依据等级；
- 做得好的部分；
- 缺失点；
- 可执行的改进建议。

### 6.4 前后测

- 前测与后测考察相同能力，但不得使用同一道问题；
- 难度和评分 Rubric 尽量保持一致；
- 后测不直接重复展示过的参考答案；
- 原题重答可以用于练习，但不得作为能力提升的核心证据；
- 后续正式实验中，评分者应尽量不知道答案属于前测还是后测。

---

## 7. 技术方案

### 7.1 技术栈

考虑到开发者熟悉 Python、Java 和 C++，但前端经验有限，首版采用单体架构：

- 后端：Python + FastAPI；
- 页面：服务端模板或简单 HTML/CSS；
- 浏览器交互：少量原生 JavaScript；
- 数据校验：Pydantic；
- 简历文本提取：PDF 使用 `pypdf` 或 `pdfplumber`，DOCX 使用 `python-docx`，TXT 按受控编码读取；
- 数据库：SQLite；
- 测试：pytest，关键流程辅以浏览器手工测试；
- 运行方式：本机单进程启动。

不采用 React、微服务、消息队列或独立 BFF。摄像头和麦克风仍需少量浏览器 JavaScript，这是 Web 媒体权限无法完全绕开的部分。

### 7.2 简历上传与解析

- 首版接受 PDF、DOCX 和 TXT；
- 单文件大小默认限制为 5 MB；
- 同时检查扩展名、MIME 类型和实际文件特征，不能只相信文件名；
- 上传文件使用随机临时文件名，禁止使用用户提供的路径或文件名写盘；
- PDF 只提取文本层，不做 OCR；
- DOCX 只读取正文和常见表格文本，不还原视觉排版；
- TXT 尝试 UTF-8，失败时给出明确错误并允许用户粘贴文本；
- 文件无有效文本、加密、损坏或格式不支持时，不调用模型，直接提示用户改用粘贴方式；
- 原始上传文件在文本提取完成后立即删除；
- 后续流程只使用提取文本、结构化画像和必要的证据片段；
- 解析结果必须由用户确认后才能生成面试计划。

### 7.3 语音与摄像头

- 摄像头通过浏览器媒体接口获取，只在本地页面预览；
- 不向后端发送视频帧；
- OpenAI 模式通过 WebRTC 连接 Realtime API，浏览器直接收发音频，不经过“识别文字后手动提交”的主链路；
- 使用语义 VAD 判断轮次结束，并允许用户说话时中断面试官当前输出；
- 默认使用自然人声，提供专业、亲和、压力三种面试官表达风格；
- 页面底部展示双方实时字幕。字幕用于可读性和记录辅助，不作为模型理解音频的唯一依据；
- 每轮通过受控工具调用将回答写入既有面试状态机，再由确定性规则控制追问次数、主题覆盖和报告生成；
- 目标是用户说完后约 1 秒内开始回应；网络、模型或设备条件不足时不承诺固定延迟；
- 不评价语速、停顿、口音和声音情绪。

开发和答辩优先使用 Chrome 或 Edge，并建议佩戴耳机以减少回声。Realtime 不可用、连接失败或麦克风不可用时，始终保留文字输入、文字展示及系统语音播报降级路径。

### 7.4 LLM 适配层

实现统一接口，不让业务逻辑绑定具体供应商：

```text
LLMClient
 ├── parse_candidate()
 ├── parse_job()
 ├── create_plan()
 ├── generate_question()
 ├── evaluate_answer()
 └── generate_report()
```

要求：

- 所有关键输出使用 JSON Schema 或 Pydantic 校验；
- 记录模型、Prompt、Schema 和 Rubric 版本；
- 校验失败最多重试一次；
- 再次失败时使用确定性降级内容；
- API Key 仅保存在本地环境变量中；
- 不提交到代码仓库；
- 免费额度耗尽时不得自动产生付费调用。

### 7.5 免费服务选择原则

项目不在计划阶段锁死模型供应商。第 1～2 天必须完成一次技术探针，对候选免费方案进行比较：

- 是否能在当前网络环境访问；
- 是否要求实名认证或绑定支付方式；
- 能否明确阻止超额计费；
- 免费额度是否足够完成开发和答辩；
- 中文及中英技术术语识别效果；
- 结构化输出稳定性；
- 平均响应延迟；
- 免费层是否会将输入用于服务改进。

选择结果写入 `docs/provider-decision.md`。如果不存在同时满足免费、稳定和隐私要求的服务，演示只能使用脱敏/虚构简历，并明确免费服务的限制。

当前电脑约为 Ryzen 5 5625U、16GB 内存和 AMD 集成显卡，不将完整本地 LLM 与高质量实时 ASR 作为主路线。

### 7.6 状态机

```text
DRAFT
  → PARSING
  → CONFIRMING
  → READY
  → DEVICE_CHECK
  → ASKING
  → LISTENING
  ↔ SPEAKING / INTERRUPTED
  → RECORDING_TURN
  → DECIDING
  → ASKING / EXTENSION_CONFIRM / REPORTING
  → FINISHED / FAILED
```

每轮保存问题、辅助转写、评价、下一动作及原因。原始音频不保存，只有已通过工具调用完整提交的轮次参与报告。

---

## 8. 最小数据模型

### 8.1 DocumentMetadata

```json
{
  "id": "document_id",
  "original_name": "resume.pdf",
  "file_type": "pdf",
  "size_bytes": 123456,
  "sha256": "hash",
  "extraction_status": "success",
  "temporary_file_deleted": true,
  "created_at": "ISO-8601"
}
```

`DocumentMetadata` 不保存原始文件路径。原始文件完成文本提取后立即删除。

### 8.2 CandidateProfile

```json
{
  "id": "candidate_profile_id",
  "target_role": "AI应用开发实习生",
  "education": [],
  "skills": [],
  "projects": [],
  "evidence_refs": [],
  "uncertain_fields": [],
  "source_document_id": "document_id",
  "source_text_hash": "hash",
  "schema_version": "v1"
}
```

### 8.3 JobProfile

```json
{
  "id": "job_profile_id",
  "title": "AI应用开发实习生",
  "mode": "validated",
  "requirements": [],
  "source_text_hash": "hash",
  "schema_version": "v1"
}
```

### 8.4 InterviewPlan

```json
{
  "id": "plan_id",
  "estimated_minutes": 30,
  "hard_limit_minutes": 60,
  "max_follow_ups_per_topic": 2,
  "topics": [],
  "required_coverage": 1.0,
  "strategy_version": "v1"
}
```

### 8.5 InterviewSession 与 Turn

```json
{
  "session": {
    "id": "anonymous_session_id",
    "status": "ASKING",
    "plan_id": "plan_id",
    "coverage": 0.5,
    "degraded": false
  },
  "turn": {
    "sequence": 3,
    "topic_id": "topic_id",
    "question": "...",
    "confirmed_answer": "...",
    "evaluation": {},
    "next_action": "probe_depth",
    "decision_reason": "缺少实际项目证据"
  }
}
```

所有关键对象带 `schema_version`、`prompt_version`、`model_version` 或 `rubric_version`，以便调试和回放。

---

## 9. 隐私与数据边界

- 摄像头只做浏览器本地预览；
- 不录像、不上传、不分析视频；
- 音频仅在当前 Realtime 会话中用于实时理解和回应，默认不保存、不落盘；
- 原始简历上传文件仅用于文本提取，提取完成后立即删除；
- 不保存上传文件的原始磁盘路径，只保存必要的文件元数据；
- 只保留结构化候选人画像和支撑解析结果的必要证据片段；
- 本地数据库只保存确认后的转写、问题、评价和报告；
- 演示资料使用脱敏或虚构数据；
- 日志不记录完整简历、音频、邮箱、电话和地址；
- 页面明确说明哪些文本或音频会发送给第三方；
- 提供删除整场面试记录的入口；
- 免费服务若可能将数据用于产品改进，不得向其发送未经脱敏的真实简历。

---

## 10. 故障与降级

| 故障 | 降级行为 |
|---|---|
| 摄像头不可用 | 关闭预览，继续语音或文字面试 |
| 麦克风不可用 | 切换文字输入 |
| Realtime 建连或断线 | 暂停计时并自动重连；持续失败则切换文字输入 |
| 实时字幕失败 | 保持语音对话，并提示字幕仅为辅助信息 |
| 实时音频输出失败 | 显示面试官文字并切换系统语音播报 |
| LLM 生成失败 | 使用当前主题的预生成候选问题 |
| 回答评价失败 | 保存回答并标记“未评分”，继续下一主题 |
| 报告生成失败 | 用已保存的结构化评价生成模板报告 |
| 网络完全中断 | 允许预生成问题演示，并标记降级状态 |

答辩前必须准备：

- 一段完整成功演示录像；
- 一份脱敏简历；
- 一份 AI 应用开发实习生 JD；
- 一份已完成的示例报告；
- 一组可离线使用的预生成问题。

---

## 11. 两周开发安排

### 第 1～2 天：技术探针与骨架

- 建立 FastAPI、简单页面和 SQLite 项目骨架；
- 验证 Chrome/Edge 的摄像头、麦克风、WebRTC Realtime 和音频播放；
- 比较至少两个可用的免费 LLM 方案；
- 验证结构化输出、中文技术回答和调用延迟；
- 冻结供应商、目标浏览器和降级方案；
- 建立代码仓库忽略规则，防止提交密钥和本地数据。

退出标准：能够在本地页面看到摄像头预览、完成一次可打断的 Realtime 语音轮次，并记录辅助字幕和结构化轮次。

### 第 3～4 天：上传、解析与确认

- 实现 PDF、DOCX、TXT 简历上传及 5 MB 大小限制；
- 实现文件类型校验、安全临时文件和解析后删除；
- 实现 PDF 文本层、DOCX 正文/表格和 TXT 文本提取；
- 保留粘贴简历文本作为失败兜底；
- 实现 JD 文本输入；
- 定义 `CandidateProfile` 与 `JobProfile`；
- 实现结构化解析、校验和一次重试；
- 实现解析结果确认与编辑；
- 加入内置的 AI 应用开发实习生基准岗位；
- 任意 JD 自动标记实验模式。

退出标准：三种支持格式均可成功提取有效文本；损坏、加密、扫描版或不支持的文件会安全失败并引导用户粘贴文本；用户能够修正画像，非法模型输出不会让流程崩溃。

### 第 5～6 天：题目基线与面试计划

- 整理首批 AI 应用开发题目和能力主题；
- 建立 A/B/C 来源分级及来源记录；
- 至少准备 4～6 个正式能力主题；
- 实现简历证据、JD 要求和岗位基线的对齐；
- 生成带权重、预计时长和完成条件的 `InterviewPlan`；
- 实现准备页和预计时长确认。

退出标准：每个正式主题都能回溯到 JD、简历或岗位基线。

### 第 7～9 天：语音面试主链路

- 实现设备检测和摄像头本地预览；
- 实现 WebRTC Realtime 会话、自然人声和三种面试官风格；
- 实现语义 VAD、用户插话打断和自动断线重连；
- 实现底部双向辅助字幕、静音与文字降级；
- 实现会话状态机与轮次持久化；
- 实现回答评价及动态动作选择；
- 加入每主题最多两次追问、预计时长和 60 分钟硬上限；
- 实现用户提前结束和延长确认。

退出标准：能够不修改数据库地手工完成一场端到端语音面试。

### 第 10～11 天：评分与报告

- 实现五级单轮与主题评分；
- 区分正式、实验、未核验和未考察；
- 显示回答证据、依据等级和置信度；
- 实现优势、问题和行动建议；
- 实现模板报告降级；
- 实现本地记录查看和删除。

退出标准：主要评价可以回溯到回答片段和 Rubric，不出现无来源的确定性结论。

### 第 12 天：前后测

- 为正式岗位准备同能力、不同题目的平行题；
- 实现一次简化后测；
- 显示前后变化，但注明样本量和限制；
- 不把原题重答算作核心提升证据。

退出标准：同一用户可以完成前测、面试训练和后测闭环。

### 第 13 天：故障、隐私与回归

- 验证摄像头、麦克风、语音、网络和模型失败路径；
- 检查日志和数据库是否泄露敏感信息；
- 验证删除记录；
- 运行核心单元测试；
- 用至少两份不同简历和 JD 回归完整流程。

退出标准：单个外部能力失败时，仍有可演示路径。

### 第 14 天：内部验收与演示材料

- 四名组员分别完成测试；
- 修复阻断流程的问题；
- 固化演示用脱敏数据；
- 录制完整成功流程；
- 保存示例报告和预生成问题；
- 记录已知限制和后续工作。

退出标准：四名组员均能独立完成完整流程，现场演示不依赖未经测试的账号、浏览器或数据。

---

## 12. MVP 验收清单

### 功能验收

- [ ] 本地应用可通过一条命令启动；
- [ ] 用户能上传 PDF、DOCX 或 TXT 简历，也能改用粘贴文本；
- [ ] 文件大小、类型和有效文本经过校验；
- [ ] 扫描版、损坏、加密或不支持的文件能安全失败并给出兜底入口；
- [ ] 原始上传文件在解析后被删除；
- [ ] 用户能确认简历解析结果和 JD；
- [ ] 正式岗位与实验岗位标识正确；
- [ ] 系统能生成 4～6 个可追溯的高优先级主题；
- [ ] 摄像头只在本地预览；
- [ ] 用户可以和 AI 面试官直接进行语音到语音交流，无需转写确认或手动提交；
- [ ] 用户插话时 AI 面试官能停止当前输出并继续理解；
- [ ] 页面底部能同步展示双方辅助字幕；
- [ ] AI 面试官能以自然人声及三种可选风格提问；
- [ ] Realtime 断线可自动重连，失败时可切换文字输入；
- [ ] 至少可稳定触发澄清、证据追问、深挖和切题；
- [ ] 预计时间、延长确认和 60 分钟硬上限生效；
- [ ] 用户提前结束时生成不完整报告；
- [ ] 未考察主题不会被判定为能力不足；
- [ ] 报告主要结论带回答证据和依据等级；
- [ ] 记录可以删除；
- [ ] 外部服务失败时存在降级路径。

### 质量验收

- [ ] 四名组员各完成 2～3 场测试；
- [ ] 无无限追问和明显重复题；
- [ ] 无阻断完整流程的 P0 问题；
- [ ] 非法结构化输出不会导致应用崩溃；
- [ ] 关键动作和失败原因可以在日志中定位；
- [ ] 演示数据全部脱敏；
- [ ] 已准备成功录屏和离线演示材料。

---

## 13. 主要风险与停止规则

### 13.1 免费服务不稳定

免费额度、访问条件和限流可能变化。第 2 天结束时仍无法稳定完成模型调用或语音转写，则立即采用：

- 浏览器语音能力 + 文字输入兜底；
- 单一可用免费模型；
- 脱敏演示数据；
- 预生成问题和模板报告。

不继续花费多日搭建本地大模型集群。

### 13.2 前端学习成本

不引入复杂前端框架。页面优先保证流程清晰、设备状态明确和演示稳定，不追求复杂动画。

### 13.3 题目真实性不足

如果正式题目没有可靠依据，则降低来源等级或移出正式评分。不得为了凑数量伪造“真实面试题”。

### 13.4 范围继续膨胀

两周内新增需求必须替换现有任务，不能只增加。以下需求自动进入后续版本：

- 数字人；
- PDF/Word 简历解析；
- 多岗位正式评分；
- 电话线路接入与多人语音房间；
- 用户账号；
- 云部署；
- 人群基准；
- 视频分析。

### 13.5 课程要求未知

课程提交物、演示网络、指定技术栈和评分标准目前未确认，这是主动接受的返工风险。任何课程硬性要求一旦明确，应优先更新本计划，而不是在开发完成后临时叠加。

---

## 14. 后续迭代

### V1：完成外部验证

- 招募真实目标学生；
- 完善 AI 应用开发岗位 Rubric；
- 引入人工盲评；
- 校准平行题难度；
- 跟踪真实面试结果。

### V1.1：体验与稳定性

- 支持常见简历文件格式；
- 改善语音识别和断线恢复；
- 增加历史训练对比；
- 云端部署和最小账号体系；
- 更完整的监控与成本控制。

### V1.2：岗位扩展

- 根据题目依据、专家资源和用户需求逐个增加正式岗位；
- 每个岗位单独建立能力模型、题目来源和评分校准集；
- 样本充足后再提供同岗位人群基准。

### V2：沉浸式交互

- 在已经证明训练闭环有效后，再评估电话线路、多方会话和更复杂的音频控制；
- 数字人和更复杂的视频体验仅作为表现层增强；
- 不将未经科学验证的表情或眼神分析用于能力评分。

---

## 15. 最终决策记录

1. 两周目标是演示级纵向原型，不是生产级平台。
2. 首个正式岗位是 AI 应用开发实习生。
3. 首版支持 PDF、DOCX、TXT 简历上传，并保留文本粘贴兜底；不做扫描件 OCR。
4. 任意 JD 可以输入，但其他岗位均为实验模式。
5. “问全”指所有高优先级主题完成有效验证，不是穷举知识。
6. 不设置固定问题总数，但设置主题数、追问次数和 60 分钟硬上限。
7. 摄像头只营造临场感，不上传、不录像、不分析。
8. 电脑端采用 OpenAI Realtime 语音到语音交互，语义 VAD 自动分轮并支持用户自由打断；底部字幕仅作辅助展示。
9. 面试过程中不公布答案，结束后统一复盘。
10. 零预算优先使用免费在线能力和浏览器能力，并准备完整降级路径。
11. 四人内部测试只证明原型可用，能力提升需要后续外部验证。
12. 课程答辩要求暂未纳入，明确接受由此产生的返工风险。
